// PRODUCTION environment.
// This file is OVERWRITTEN by generate-env.js during "npm run build".
// The values below are only placeholders for a fresh clone.
export const environment = {
  apiUrl: 'https://example-api.com',
  appEnv: 'production'
};
